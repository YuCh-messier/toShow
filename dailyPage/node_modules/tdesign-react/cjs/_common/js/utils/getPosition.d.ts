declare type Placement = 'top' | 'bottom' | 'left' | 'right';
declare const getPosition: (targetEle: HTMLElement, contentEle: HTMLElement, placement: Partial<Placement>) => {
    left: number;
    top: number;
};
export default getPosition;
